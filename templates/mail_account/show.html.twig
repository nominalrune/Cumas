{% extends 'base.html.twig' %}

{% block title %}MailAccount{% endblock %}

{% block body %}
    <h1>MailAccount</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mail_account.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ mail_account.name }}</td>
            </tr>
            <tr>
                <th>POP Server</th>
                <td>{{ mail_account.popServer }}</td>
            </tr>
            <tr>
                <th>POP Port</th>
                <td>{{ mail_account.popPort }}</td>
            </tr>
            <tr>
                <th>SMTP Server</th>
                <td>{{ mail_account.smtpServer }}</td>
            </tr>
            <tr>
                <th>SMTP Port</th>
                <td>{{ mail_account.smtpPort }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ mail_account.username }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_mail_account_index') }}">back to list</a>

    <a href="{{ path('app_mail_account_edit', {'id': mail_account.id}) }}">edit</a>

    {{ include('mail_account/_delete_form.html.twig') }}
{% endblock %}
